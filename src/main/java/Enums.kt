enum class LocatorType {
    ID,
    XPATH,
    ACCESSIBILITY_ID,
    CLASS_NAME,
    IOS_CLASS_CHAIN,
    IOS_PREDICATE_STRING
}

enum class TypeOS {
    ANDROID,
    IOS
}
// класс перечислений локаторов